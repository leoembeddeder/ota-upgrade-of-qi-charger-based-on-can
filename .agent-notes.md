# 项目上下文摘要（给新会话 coder 快速恢复用）

> 最后更新：2026-09-23。技术细节权威来源：`docs/16. 功能需求文档.md`（架构/协议/需求）与 `docs/3. 合并-CAN协议-UDS-OTA工作流.md`（实操工作流）；本文件只做导航与约定速记，冲突时以 docs/ 为准。

## 1. 项目一句话定位

基于 AT32F426 的车载 Qi 无线充电模块（MCU2），通过 CAN 2.0B（29-bit 扩展帧, 250kbps）实现 UDS 诊断 + Boot/App/备份区单 App OTA 固件升级；同时经 UART 与 Qi 芯片通信（充电控制 + Qi 芯片自身 IAP）。

## 2. 目录结构地图

| 路径 | 职责 |
|------|------|
| `qi_wireless_bootloader/` | Bootloader Keil 工程（16KB @0x08000000）：镜像验签（ECDSA P-256/uECC）、备份区→App 区物理搬运、Safe Mode 挂起；**无 UDS 服务**。子目录 `mdk_project`(Keil) / `mdk_user`(main/时钟/中断) / `mdk_can` / `mdk_app` / `libraries` |
| `qi_wireless_code_app/` | APP Keil 工程（App 区 48KB, IROM1=`0x08004100`）：Qi 充电业务 + CAN 生命周期广播 + UDS 诊断 + OTA 下载（写备份区）。位置无关固件，只维护这一份源码 |
| `qi_wireless_code_app/mdk_app/Src` | 协议与业务核心（见第 4 节路径清单） |
| `python_tools/` | 工具链：打包签名（`1.packaging script/`）、功能测试（`2.functional test script/`）、一键 OTA/压力测试/Qi IAP 脚本（根目录）、Qi 芯片 IAP 固件（`iap bin/log1.BIN|log2.BIN`） |
| `docs/` | 编号文档 1~16 + `keys/`（ECDSA P-256 密钥对）+ `pdf/`（原理图等） |
| `AGENTS.md` | **仓库代理规则（必读）**：唯一改动源、提交规范、版本号联动、log1/log2 同步 |
| `review_reports/` | 历史审查报告（如 OTA-ARCH-0920 metadata 链审计） |
| `README.md` | 版本跟踪 + 功能开发状态（架构细节不在此） |

架构基线：**OTA-ARCH-0920**（2026-09-20 起）——A/B 双槽永久取消，改为 Boot + App + 备份区（`0x08010000..0x0801BFFF`）单 App 升级流：APP 内 0x31/0x34/0x36/0x37 下载到备份区 → 0x37 验签提交 metadata 后 `NVIC_SystemReset`（**不再自复位切槽，等主机发 11 01**）→ Boot 下次上电执行备份区→App 区搬运并复验。

## 3. 关键协议要点

### 3.1 QI 芯片协议（qi_protocol, USART2 9600 8N1）

- 帧格式：`帧头 55 AA + 长度(1B) + 命令(1B) + 数据(nB) + 流水号(1B) + 校验(1B)`；IAP 帧无流水号。
- 命令码：`0x00` ACK / `0x01` Qi 主动上报（充电状态、版本）/ `0x02` 设置功率 / `0x03` 查询固件版本（主动问询，DID 0x2013 链路）/ `0xCC` IAP 升级。
- Qi 芯片 IAP 经 UDS DID `0x2130`（控制）/ `0x2131`（数据）/ `0x2132`（状态）桥接，配套脚本 `zcanpro_qi_iap_log1.py` / `log2.py`。

### 3.2 UDS/CAN 协议（can_protocol）

CAN ID：

| ID | 方向 | 用途 |
|----|------|------|
| `0x18DA0D03` | CCU→MCU | UDS 物理寻址请求 |
| `0x18DA030D` | MCU→CCU | UDS 物理寻址响应 |
| `0x18DB33F1` / `0x18DB33xx` | 广播 | UDS 功能寻址 |
| `0x18FF260D` | MCU→总线 | 生命周期状态广播 |
| `0x18FF480D` | Boot | 开机决策链诊断帧 M1~M4 |

UDS 服务：0x10 / 0x11（ECUReset，11 01 精确等待修复）/ 0x22 / 0x2E / 0x27（SecurityAccess 32B seed，ECDSA 签名）/ 0x31（例程：擦备份区、0x2100 Clear Faults）/ 0x34 / 0x36 / 0x37（收尾不自行复位，等主机 11 01）/ 0x3E。NRC 0x78 ResponsePending 走 `can_proto_pump_long_op`（4500ms 补发阈值）。

重点 DID（全表见 `can_protocol.h` 122~158 行）：

| DID | 含义 |
|-----|------|
| `0xF195` | APP 版本（取编译常量 `SW_VERSION_STR` = `QC_JYF_FW_1.1.1`，不读镜像头/metadata） |
| `0xF180` / `0xF193` / `0xF18C` | Bootloader 版本 / 硬件版本 / SN |
| `0x2013` | **Qi 芯片版本主动问询**：先 `7F 22 78` 延迟应答，Qi 回复后 `62 20 13` + ASCII `QC_JYF_MCU2_FW_1.1.X`（X=Qi 版本整数的十进制数字，变长 23~27B，超时有缓存兜底；组合读含 0x2013 回 NRC 0x22） |
| `0x2112` | OTA 状态八态机：Idle/Downloading/Validating/PendingActivation/TrialBoot/Confirmed(0x05)/RolledBack/Failed |
| `0x2113` / `0x2114` | 当前活动槽 / pending 槽（架构重构后仍保留读取，作兼容 shim） |
| `0x2130~0x2133` | Qi IAP 控制/数据/状态/版本（0x2133 版本来自 0x01 上报缓存） |
| `0x2100~0x2119` | 充电能力/使能/状态/功率限制等业务 DID（详见头文件） |

### 3.3 ISO-TP 多帧路径

- 实现：`isotp.c`（ISO 15765-2），两个工程各一份；FC 发送有竞态修复（确认送达+重试 ≤3 次+双邮箱回退+发帧落地等待 8ms/5ms，参数只在 `.c` 内部，不动 `isotp.h` 导出面）。
- **发送多帧的唯一安全路径**：`proto_send_response` 延迟发送机制——`g_tx_pend` 置位 → `can_protocol_poll` 末尾 `proto_flush_pending_tx` 发送；帧回调上下文内**禁止**直接 `isotp_tx_send`（27 服务 34B seed/key 与 0x2013 23~27B 应答均走此路径）。
- CF 必须等上一帧上总线（PTB 优先于 STB），否则 SN 乱序、主机表现为 `22 F195` 无应答。

## 4. 关键文件路径清单

| 文件 | 内容 |
|------|------|
| `qi_wireless_code_app/mdk_app/Src/can_protocol.c` + `Inc/can_protocol.h` | UDS 服务与 DID 全部实现；`SW_VERSION_STR` 等版本宏唯一定义处；DID 宏表（h 文件 122~158 行） |
| `.../Src/qi_protocol.c` + `Inc/qi_protocol.h` | Qi UART 协议解析/命令收发（含 0x03 版本查询） |
| `.../Src/qi_uart.c` | USART2 底层驱动 |
| `.../Src/ota_download.c` | UDS 0x31/0x34/0x36/0x37 下载到备份区、验签、metadata 提交、复位；有界擦除预算（TC-1307） |
| `.../Src/ota_trigger.c` | metadata 双副本读写 + CRC32（`ota_metadata_t`, magic "MATO" 28B） |
| `.../Src/isotp.c` | ISO-TP 传输层（FC 竞态修复现场） |
| `.../Src/lifecycle.c` / `nvm_drv.c` / `device_info.c` / `board_gpio.c` | 生命周期广播 / NVM / SN 存储 / GPIO |
| `qi_wireless_bootloader/mdk_app/Src/boot_verify.c` / `boot_metadata.c` / `boot_jump.c` / `boot_safe_mode.c` | Boot 验签 / metadata / 跳转 / Safe Mode（M1~M4 诊断帧） |
| `python_tools/zcanpro_ext_ota_auto.py` | 唯一 OTA 主脚本（三值解包 to_slot/got_ver/ota_status + 0x2112==0x05 确认闭环） |
| `python_tools/1.packaging script/pack_image.py` 等 | 打包 XATO 头(256B)/合并产线镜像/校验/seed 签名 |
| `python_tools/2.functional test script/zcanpro_read_app_version.py` | 读版本（0xF195/0xF180/0xF193） |
| `python_tools/zcanpro_ota_stress_test.py` | OTA 压力测试 |
| `docs/10. CAN-UDS OTA 测试用例表.md` | 测试用例定义（v2.0，69 条；只保留用例定义，无历史记录） |

## 5. 既有重要决策 / 约定（改前必读）

1. **唯一改动源**：只改 WSL 侧本仓库 `/home/whites/embedded_item/ota-upgrade-of-qi-charger-based-on-can`，改完必须 `git push`；**Windows 端一切路径不触碰**（用户自行 pull）。
2. **提交规范**：每次改完 `git add -A` 全工程提交；commit summary 简洁概括，**Description 按模块/文件分点**列具体改动（关键地址/常量/新增函数名），如 `feat(UDS): ...` / `fix(OTA): ...` / `docs(...)` conventional 前缀。
3. **版本号唯一真相源**：`SW_VERSION_STR`（`qi_wireless_code_app/mdk_app/Src/can_protocol.c`）；发版只改固件常量 + docs；XATO 镜像头 version 区（0x4C）固定填 0x00 不携带版本；独立版本族：`BOOTLOADER_VER_STR`、`HW_VERSION_STR`、Qi IAP `EXPECTED_FW_VERSION`。
4. **log1/log2 同步铁律**：`zcanpro_qi_iap_log1.py` 与 `log2.py` 仅允许 4 处差异（头注释固件名/`FIRMWARE_NAME`/`EXPECTED_FW_VERSION`/互斥说明），改任一必须同步另一，`diff` 验证并在 commit message 注明 `log1 + log2 已同步`。
5. **代码注释**：固件注释以英文为主（文件头、流程约束、行内 `/*!< */` 关键行为说明），近期多次审查（evaluator）对**注释失实**判 FAIL——注释必须与实际行为一致，改行为必须同步注释。
6. **单 App 架构残留红线**：APP 永不擦自己运行的区域；备份区→App 区搬运只由 BOOT 执行且复验后才清 pending；产线烧录用 `merge_prod_bin.py` 合并镜像，备份区出厂为空。
7. `embedded_lib/` 等只读库不改；本工程内 `libraries/` 为 CMSIS + AT32 SPL。
8. **测试验证手段**：无固定测试框架；历史上用 gcc 桩测试（awk 从源文件机械提取真实函数体 + `gcc -std=c99 -Wall -Wextra -Werror -fsanitize=address,undefined` 编译断言，如 0x2013 的 48/48 PASS），脚本临时生成不入库；硬件侧用 ZCANPRO 导入 `python_tools/` 下脚本跑用例表。

## 6. 常用构建 / 测试命令

```bash
# 固件编译（Keil MDK，无 CLI 构建脚本）
#   Boot: 打开 qi_wireless_bootloader/mdk_project/qi_wireless.uvprojx, IROM1=0x08000000/0x4000
#   APP : 打开 qi_wireless_code_app/mdk_project/qi_wireless_code_app.uvprojx, IROM1=0x08004100/0xBF00
#   （Linker 勾选 Use Memory Layout from Target Dialog；产物 Objects/*.bin）

# 打包 / 校验 / 合并（在 python_tools/ 下执行）
python "1.packaging script/pack_image.py"            # 裸 bin → XATO 头 .ota.bin
python "1.packaging script/verify_image.py" "app bin/app_image.bin"
python "1.packaging script/merge_prod_bin.py"        # Boot + APP 产线合并镜像
python "1.packaging script/sign_seed.py" <seed_hex>  # SecurityAccess seed 签名

# OTA / 测试（ZCANPRO → 高级功能 → 扩展脚本 → 打开运行）
#   zcanpro_ext_ota_auto.py      一键 OTA（升级后自动读 DID 确认：APP 应答 + 0x2113 + 0xF195 + 0x2112==0x05）
#   zcanpro_ota_stress_test.py   OTA 压力循环
#   zcanpro_qi_iap_log1.py / log2.py  Qi 芯片 IAP
#   2.functional test script/ 下：版本读取、SN、充电控制等

# log1/log2 同步校验
diff python_tools/zcanpro_qi_iap_log1.py python_tools/zcanpro_qi_iap_log2.py
```

当前红项（README §6.2）：0x31 擦除 + 0x37 非阻塞 0x78/0x77 在真实总线压力下的 Bus-Off 稳定性回归，是 MCU OTA 链路唯一未关闭项。
