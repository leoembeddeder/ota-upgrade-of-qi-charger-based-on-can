# 宏定义切换方式

> 项目中所有可通过宏定义切换的行为模式汇总
> 更新：2026-09-02；2026-09-18 增补 §7 CAN_LP_STANDBY_ENABLE（回归调试期临时开关）；2026-09-18 §2/§3 safe mode 两宏确认已移除（17bc8ab），行为固化描述同步 HEAD

---

## 使用方法

在 Keil `.uvprojx` → **C/C++** → **Define** 中添加宏，多个用逗号分隔：

```
BOOT_JUMP_METHOD=1
```

或在源文件顶部 `#define`（仅对该文件生效）。

---

## 1. BOOT_JUMP_METHOD

| 项目 | 说明 |
|------|------|
| 文件 | `qi_wireless_bootloader/mdk_app/Src/boot_jump.c` |
| 默认值 | `0` |
| 取值 | `0` 或 `1` |

| 值 | 方式 | 说明 |
|----|------|------|
| `0` | 内联汇编 | `msr msp` + `bx`，编译器无关，当前默认方式 |
| `1` | 函数指针 | `__set_MSP` + `((app_entry_t)(reset_addr))()`，官方 IAP 风格 |

**代码位置**：
```c
#ifndef BOOT_JUMP_METHOD
#define BOOT_JUMP_METHOD  0
#endif
```

**切换方式**：Keil Define 中加 `BOOT_JUMP_METHOD=1`，或改默认值为 `1`。

**差异说明**：
- 方式 0：`msp` 和 `reset` 通过内联汇编寄存器传入，跳转后不依赖 C 栈，更安全
- 方式 1：先调 `__set_MSP` 再通过函数指针跳转，与官方例程一致，可读性更好

---

## 2. SAFE_MODE_PROBE_ENABLE（已移除，行为固化）

> **已移除**：该宏连同 SAFE_MODE_PROBE_PERIOD_MS / SAFE_MODE_PROBE_STATE
> 于 17bc8ab 从 boot_safe_mode.c 删除（HEAD 全树 grep 零引用）。心跳改为
> **无条件**发送，不再有编译开关。

**现行 HEAD 行为（boot_safe_mode.c enter_safe_mode / safe_heartbeat）**：
Safe Mode 无条件每 500ms 在 `0x18FF260D`（CAN_ID_LIFECYCLE_BROADCAST）发送
心跳帧 `01 41 42 54 cause fail_step A5 00`（'ABT' 标记 + cause/step 现场），
并同周期调用 `sit1145_normal_mode_set()` 重切收发器 Normal（见 §3）。
帧格式注释以 boot_safe_mode.c 头部与 python_tools/zcanpro_ext_ota_auto.py
头部 SAFE_MODE 注释为准（两侧同步，改一侧必同步另一侧）。

| 项目 | 说明 |
|------|------|
| 文件 | `qi_wireless_bootloader/mdk_app/Src/boot_safe_mode.c` |
| 现状 | 宏已删除，心跳无条件启用（节拍等效旧取值 `1`，帧内容已不同：旧文档 byte[0]=0x00 探针帧已不存在） |

---

## 3. SAFE_MODE_SIT1145_KEEPALIVE_ENABLE（已移除，行为固化）

> **已移除**：该宏连同 SAFE_MODE_SIT1145_KEEPALIVE_PERIOD_MS 于 17bc8ab
> 从 boot_safe_mode.c 删除（HEAD 全树 grep 零引用）。keepalive 改为**无条件**执行。

**现行 HEAD 行为**：enter_safe_mode 主循环每 500ms 无条件调用
`sit1145_normal_mode_set()`，与心跳帧同一节拍（见 §2）。Flash 写操作期间
CPU 停滞，SIT1145 可能因 SPI 无响应掉入 Standby，keepalive 在停滞结束后
恢复 Normal 的设计意图不变，只是不再可编译关闭。

| 项目 | 说明 |
|------|------|
| 文件 | `qi_wireless_bootloader/mdk_app/Src/boot_safe_mode.c` |
| 现状 | 宏已删除，keepalive 无条件启用（等效旧默认值 `1`） |

---

## 4. ENTRY_MODE（已废弃）

> **已废弃**：脚本收敛为单一 `zcanpro_ext_ota_auto.py`，ENTRY_MODE 三种入口模式（auto/app/boot）已移除。
> 现在 OTA 全程在 APP 内完成（0x31/0x34/0x36/0x37），Boot 无 UDS。

| 项目 | 说明 |
|------|------|
| 文件 | `python_tools/zcanpro_ext_ota_auto.py` |
| 默认值 | `"auto"` |
| 取值 | `"auto"` / `"app"` / `"boot"` |

| 值 | 说明 |
|----|------|
| `"auto"` | 【已废弃，行为描述仅存历史意义】旧实现发 `0x34` 按 NRC 值分叉定位（"NRC `0x11`=已在 APP"），该判据与现固件相反且不得回退：APP 默认会话对 `0x34` 回 NRC `0x22`（`ota_download.c` 会话门禁），对 `0x34` 的任何应答=APP，Boot 对 `0x34` 静默。现行判定只按正证据（见 `python_tools/2.functional test script/zcanpro_charge_start.py probe_location`）：`22 2113` 应答且 `62 21 13` 后字节为 `0xFE`=Boot safe mode；任何其他应答（含任意 NRC）=APP；无应答先 Standby 唤醒收敛+生命周期帧监听再判 |
| `"app"` | 脚本先尝试让 APP 进 Boot（历史模式：旧流程发 `10 02`+`27`+`11 01`；`11 01` 已删除 2026-09-23），再进 Programming |
| `"boot"` | 脚本直接假设 MCU 已在 Boot Safe Mode，跳过 APP→Boot 步骤 |

**代码位置**：
```python
# ENTRY_MODE 已废弃（脚本收敛为单一 zcanpro_ext_ota_auto.py）
```

**用途**：`"boot"` 用于 MCU 已在 Safe Mode 时直接 OTA（跳过等待）。`"app"` 用于正常从 APP 触发升级。`"auto"` 兼容两种场景。

---

## 5. TRANSFER_BLOCK_DATA（脚本侧）

| 项目 | 说明 |
|------|------|
| 文件 | `python_tools/zcanpro_ext_ota_auto.py` |
| 默认值 | `128` |
| 取值 | `1` ~ `254` |

| 值 | 说明 |
|----|------|
| `128` | 每帧传输 128 字节（默认，平衡速度与可靠性） |
| `254` | 最大值，每帧传最多数据（ISO-TP SF 上限） |
| `64` | 更保守，每帧更少数据 |

**代码位置**：
```python
TRANSFER_BLOCK_DATA = 128
```

**注意**：Bootloader 的 `UDS_MAX_BLOCK_LEN` = 256，`TRANSFER_BLOCK_DATA` 不应超过 `256 - 2`（SID + BSC）= 254。

---

## 6. IMAGE_MAGIC / IMAGE_HEADER_SIZE（镜像格式）

| 项目 | 说明 |
|------|------|
| 文件 | `python_tools/zcanpro_ext_ota_auto.py` + Boot `boot_metadata.h` |
| IMAGE_MAGIC | `0x4F544158`（"XATO"） |
| IMAGE_HEADER_SIZE | `256` 字节 |

这两个值在脚本和 Boot 两侧必须一致。修改需同步更新：

| 文件 | 用途 |
|------|------|
| `python_tools/zcanpro_ext_ota_auto.py` | `IMAGE_MAGIC`, `IMAGE_HEADER_SIZE` |
| `qi_wireless_bootloader/mdk_app/Inc/boot_metadata.h` | `IMAGE_HEADER_SIZE` |
| `qi_wireless_bootloader/mdk_app/Inc/boot_verify.h` | `IMAGE_MAGIC`（`ECDSA_PUBKEY_MAGIC`） |

---

## 7. CAN_LP_STANDBY_ENABLE（APP 固件，回归调试期临时开关）

> **临时禁用：回归调试期设备保持 CAN 常在线；生产恢复改 1**（2026-09-18）

| 项目 | 说明 |
|------|------|
| 文件 | `qi_wireless_code_app/mdk_app/Inc/can_protocol.h`（:63） |
| 当前值 | `0`（回归调试期临时禁用） |
| 取值 | `0` 或 `1` |

| 值 | 说明 |
|----|------|
| `1` | 历史行为：非 trial/rollback 上电保持 Standby；空闲 180s（`CAN_LP_IDLE_TIMEOUT_MS`）自动进 Standby；`sit1145_init()` 末尾收发器进 Standby |
| `0` | 所有进入 Standby 的入口关闭，上电/复位/空闲均保持 CAN 常在线；唤醒/恢复代码全部保留不删，改回 `1` 行为与历史完全一致 |

**受控入口（开关=0 时全部关闭）**：

1. **上电/复位默认 Standby**：`can_protocol_init()` 非 trial 分支（can_protocol.c :1916-1922）——开关=0 改置 `g_lp_need_online=1`，首次 `can_protocol_poll` 延时 `can_lp_enter_normal()`（SysTick 就绪后，与 trial 同路径）
2. **空闲超时自动进 Standby**：`can_protocol_poll()` 180s 判定（can_protocol.c :2080-2086）——`#if` 加开关条件，=0 时整段不编译
3. **`sit1145_init()` 步骤10 收发器进 Standby**（sit1145.c :1091）——`#if` 跳过；宏未定义时保持进入 Standby（生产语义，防误配）
4. **函数定义**：`can_lp_enter_standby`/`can_lp_hold_standby`（can_protocol.c :351-377）随开关不编译，源码完整保留

**行为对照**：

| 场景 | 开关=1（历史） | 开关=0（回归调试） |
|------|----------------|--------------------|
| 上电/复位 | trial/rollback 在线；其余保持 Standby | 一律首次 poll 进 Normal，常在线 |
| 空闲 180s | 发 SB 标记→收发器进 Standby | 不进，保持在线 |
| OTA 复位后 | trial/rollback 在线；其余 Standby | 一律在线 |
| trial 上电 | 在线（confirm 需要 CAN） | 在线（流程不变，只更可靠） |

**入口复核（grep 全量）**：`can_lp_enter_standby` 调用点仅空闲超时（已守卫）；`can_lp_hold_standby` 调用点仅 init 分支 + enter_standby 函数体（均已守卫）；`sit1145_standby_mode_set` APP 侧调用点仅 sit1145.c 步骤10 + hold_standby 函数体（均已守卫）；`pwc_standby_mode_enter`/`can_standby_mode_enable` 应用层无调用；bus-off 恢复兜底 `can_driver_offline→online`（can_protocol.c :469）立即回在线，非 Standby 入口。**Boot 侧无 standby 进入逻辑**（boot sit1145_init 以 normal_mode_set 结尾，Safe Mode keepalive 默认=1），无需改。

**诊断影响**：DID `0x2119`（SIT1145 LP 状态）读数恒为 ever_standby=0 / wup_cnt=0 / last_standby_sec=0——开关语义，非故障；生命周期 SB 关机标记（`06 41 53 42`）不再出现。

**MDK 验证（烧录后）**：

1. qi_wireless_code_app Rebuild 编译 0 error（开关=0 下 standby 函数不编译，应无 unused 警告）
2. 上电后立即用 `zcanpro_read_app_version.py` 读三 DID（0xF195/0xF180/0xF193）全部有应答
3. 放置超过 3 分钟后再读仍有应答（空闲不再进 Standby）
4. 完整跑一轮 OTA 升级，trial/confirm 流程正常

**待烧台账条目（2026-09-18）**：qi_wireless_code_app（原 slotA）需 MDK Rebuild + **烧录器直烧**（设备现跑旧固件会 180s 后休眠，无法 OTA 自举）；烧录基线 commit = `e28235c`（standby 开关代码随该提交入库）；生产恢复时改 `CAN_LP_STANDBY_ENABLE=1` 重编重烧；`SW_VERSION_STR` 未改，台账以 commit 号区分烧录物。

**归属说明**：standby 开关代码（can_protocol.h/c、sit1145.c）于 2026-09-18 11:40 被并行打包脚本改名会话的 `git add -A` 一并扫入 e28235c（其 message 仅描述脚本改名）；开关技术细节以本节与本日 docs/14 补录提交 Description 为准。

---

## 8. 宏切换速查表

| 宏名 | 文件 | 默认值 | 取值 | 说明 |
|------|------|--------|------|------|
| `BOOT_JUMP_METHOD` | boot_jump.c | `0` | 0/1 | 跳转 APP 方式：0=汇编，1=函数指针 |
| `SAFE_MODE_PROBE_ENABLE` | boot_safe_mode.c | **已移除**（17bc8ab） | — | Safe Mode 心跳已无条件启用（0x18FF260D 每 500ms `01 41 42 54 cause step A5 00`，见 §2） |
| `SAFE_MODE_SIT1145_KEEPALIVE_ENABLE` | boot_safe_mode.c | **已移除**（17bc8ab） | — | SIT1145 保持 Normal 已无条件启用（每 500ms sit1145_normal_mode_set，见 §3） |
| `TRANSFER_BLOCK_DATA` | zcanpro_ext_ota_auto.py | `128` | 1~254 | 每帧传输字节数 |
| `CAN_LP_STANDBY_ENABLE` | can_protocol.h（code_app） | `0`（临时） | 0/1 | APP CAN Standby 进入总开关；回归调试期=0 常在线，**生产恢复改 1**（见 §7） |

---

## 9. Keil 工程配置示例

在 `.uvprojx` 的 **C/C++** → **Define** 中：

```
BOOT_JUMP_METHOD=1
```

效果：
- Bootloader 跳 APP 使用函数指针方式（官方 IAP 风格）
- SAFE_MODE_PROBE_ENABLE 已于 17bc8ab 移除：Safe Mode 心跳与 SIT1145 keepalive为无条件行为，无需配置

生产环境建议：
```
BOOT_JUMP_METHOD=0
```

效果：
- 使用内联汇编跳转（更安全）
- Safe Mode 心跳与 SIT1145 keepalive 为无条件行为（非宏控制，见 §2/§3）
