# python_tools — 产线与调试工具集

本目录包含 Qi 无线充电模块 CAN-OTA 项目的全部 Python 工具脚本。

**脚本可从任意目录执行**。脚本通过 `__file__` 自动推算仓库根路径，固件/密钥等默认路径不受 `cd` 位置影响。

> ⚠️ 注意：命令行传入的路径参数（如 `--bin`、`--out`）是相对于**当前工作目录**的。如果不在仓库根执行，需要用绝对路径或相对于当前位置的路径。

---

## 脚本总览

| 脚本 | 功能 | 典型场景 |
|------|------|----------|
| `pack_image_slotA.py` | 裸 bin → 签名 .ota.bin（XATO 头 + CRC32 + ECDSA P-256），版本 1.1.1 | 打包 Slot A |
| `pack_image_slotB.py` | 裸 bin → 签名 .ota.bin（XATO 头 + CRC32 + ECDSA P-256），版本 1.1.2 | 打包 Slot B |
| `zcanpro_ext_ota_auto.py` | ZCANPRO 扩展脚本，自动识别 A/B 槽升级 | 开发调试 OTA |
| `zcanpro_ext_ota_slotA_1_1_1.py` | ZCANPRO 扩展脚本，升级 Slot A（1.1.1） | OTA Slot A |
| `zcanpro_ext_ota_slotB_1_1_2.py` | ZCANPRO 扩展脚本，升级 Slot B（1.1.2） | OTA Slot B |
| `zcanpro_qi_iap_log1.py` | Qi 芯片 IAP 固件升级（log1.BIN，V1.1） | Qi 芯片升级 |
| `zcanpro_qi_iap_log2.py` | Qi 芯片 IAP 固件升级（log2.BIN，V1.2） | Qi 芯片升级 |
| `zcanpro_qi_read_status.py` | 查询 Qi 芯片 IAP 升级状态 | 调试 IAP 状态 |
| `zcanpro_qi_read_version.py` | 读取 Qi 芯片固件版本（DID 0x2133） | 调试版本确认 |
| `zcanpro_qi_set_power.py` | 设置 Qi 芯片功率（5W/10W/15W） | 调试功率控制 |
| `zcanpro_charge_start.py` | 启动 Qi 无线充电（DID 0x2101 使能） | 调试充电功能 |
| `zcanpro_read_app_version.py` | 读取 APP 侧版本号 DID 0xF195 / 0xF180 / 0xF193 | 调试版本确认 |
| `zcanpro_sn_write.py` | SN 序列号写入（DID 0xF18C） | 产线写 SN |
| `merge_prod_bin.py` | Bootloader + APP 合并为单一产线镜像 | 产线烧录前打包 |
| `verify_image.py` | XATO 镜像完整性 + 签名校验 | 打包后自检 / 产线抽检 |
| `sign_seed.py` | SecurityAccess seed 签名生成 + CAN 帧输出 | 手动测试 0x27 解锁 |

---

## 1. 镜像打包脚本（pack_image）

把 Keil 裸 APP `.bin` 打成带 256 字节 XATO 头的 `.ota.bin`，含 CRC32 + ECDSA P-256 签名。不需要 ZCANPRO 环境。

两个版本功能相同，仅写入镜像头的版本号不同。

### 用法

```bash
python python_tools/pack_image_slotB.py

# Slot A 固件 → pack_image_slotA.py（版本 1.1.1）
python python_tools/pack_image_slotA.py \
  --bin qi_wireless_code_slotA/mdk_project/Objects/qi_wireless.bin \
  --version 1.1.1

# Slot B 固件 → pack_image_slotB.py（版本 1.1.2）
python python_tools/pack_image_slotB.py \
  --bin qi_wireless_code_slotB/mdk_project/Objects/qi_wireless.bin \
  --version 1.1.2
```

### 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--bin` | `qi_wireless_code_slotA/.../qi_wireless.bin` | Keil fromelf 产出的裸 APP .bin |
| `--key` | `docs/keys/private.pem` | ECDSA P-256 私钥 PEM |
| `--out` | 自动（见下方说明） | 输出镜像路径 |
| `--version` | `1.0.0` | 写入头的版本字符串 |

### 自动输出路径

`--out` 不指定时，脚本根据镜像的 Reset Handler 地址自动判断槽号，输出到：

| 槽 | 输出路径 |
|----|----------|
| Slot A | `python_tools/app bin/app_slot_a.bin` |
| Slot B | `python_tools/app bin/app_slot_b.bin` |

### 输出布局

```
app_slot_a.bin:
  [0x0000]  XATO Header (256 bytes)
            Magic(4B) + Length(4B) + CRC32(4B) + Signature(64B) + Version(16B) + Timestamp(4B) + Padding
  [0x0100]  Firmware payload (裸 bin 原始数据)
```

产线烧录地址：**`0x08007000`**（含 256 字节头）。Bootloader 烧到 `0x08000000`。

---

## 2. OTA 脚本（ZCANPRO 扩展脚本）

在 ZCANPRO 里一键完成 MCU CAN-UDS OTA，不用手动点 UDS、也不用自己组 ISO-TP。

> **Python**: ZCANPRO 要求 **Python 3.8 32 位**
> **入口**: ZCANPRO → 高级功能 → 扩展脚本 → 打开对应文件
> **总线**: Classical CAN 2.0B，250 kbps，29-bit 扩展帧

### 2.1 脚本对照

| 脚本 | 用途 | 固件来源 | 适用场景 |
|------|------|----------|----------|
| `zcanpro_ext_ota_auto.py` | 自动识别 A/B 槽 | 扫描 `app bin/` 目录下已打包的 bin | 不确定当前槽，自动选择 |
| `zcanpro_ext_ota_slotA_1_1_1.py` | 指定升级 Slot A | `qi_wireless_code_slotA/.../qi_wireless.bin`（自动打包） | 当前 APP 在 Slot B，升级 Slot A |
| `zcanpro_ext_ota_slotB_1_1_2.py` | 指定升级 Slot B | `qi_wireless_code_slotB/.../qi_wireless.bin`（自动打包） | 当前 APP 在 Slot A，升级 Slot B（最常见） |

### 2.2 auto 脚本使用

auto 脚本扫描 `app bin/` 目录，通过 Reset Handler 地址自动识别 Slot A/B。

使用前需要先用 `pack_image` 打包：

```bash
# 先打包到 app bin/
python python_tools/pack_image_slotA.py --bin qi_wireless_code_slotA/mdk_project/Objects/qi_wireless.bin
python python_tools/pack_image_slotB.py --bin qi_wireless_code_slotB/mdk_project/Objects/qi_wireless.bin

# 然后在 ZCANPRO 中运行 zcanpro_ext_ota_auto.py
```

### 2.3 OTA 流程

1. （可选）让正在跑的 APP 进 Bootloader（`RESET_APP_TO_BOOT = True`）
2. Programming Session + ECDSA P-256 解锁（`27 01` → `27 03` 分帧送签名 → `27 02` 验签）
3. 擦除非活跃槽，读 DID `0x2114` 核对目标槽
4. 分块下发镜像（`0x34` + `0x36`），`TransferExit` 验签
5. 复位后确认已在 APP

### 2.4 脚本顶部配置

```python
RESET_APP_TO_BOOT = True       # APP 在跑则先复位进 Boot
DOWNLOAD_ADDR = 0x08007000     # 兜底；实际用 DID 0x2114 的槽基址
TRANSFER_BLOCK_DATA = 128      # 每块数据字节，1..254
```

### 2.5 Slot 对照

| 槽 | Keil Target IROM1 Start | IROM1 Size | 适用场景 |
|----|------------------------|------------|----------|
| A | `0x08007100` | `0xA700` | 空片首次下载，或当前活跃槽为 B |
| B | `0x08011900` | `0xA700` | 当前 APP 跑在 Slot A（最常见） |

### 2.6 CAN 诊断 ID

| 方向 | CAN ID |
|------|--------|
| 主机 → MCU | `0x18DA0D03` |
| MCU → 主机 | `0x18DA030D` |

### 2.7 失败对照

| 日志/现象 | 原因 | 处理 |
|-----------|------|------|
| 请先打开 CAN 通道 | 设备未启动 | 先打开 250kbps 扩展帧通道 |
| 找不到固件 / 找不到私钥 | 路径错误 | 检查 Keil 编译输出路径 |
| UDS 无应答 | 波特率/帧类型错、接线 | 250k 扩展帧 |
| NRC `0x35` | 私钥和 MCU 公钥不匹配 | 换密钥或重编 Bootloader |
| NRC `0x72` | CRC/签名失败，或镜像链接地址不在本槽 | 用对应槽的 bin |
| Reset Handler 不在 Slot A/B 内 | Keil IROM1 地址设错 | Slot A = `0x08007100`，Slot B = `0x08011900` |
| 复位后无 UDS | 跳转失败或 APP CAN 未起来 | 检查 Bootloader 版本和镜像完整性 |

---

## 3. Qi IAP 脚本 — Qi 芯片固件升级

通过 CAN-UDS 将 Qi 无线充芯片固件推送给 MCU，MCU 再通过 UART 转发给 Qi 芯片。

两个脚本分别对应不同版本的 Qi 芯片固件，**除固件文件名和版本号外完全一致**（见 `AGENTS.md` 同步规则）。

### 3.1 脚本对照

| 脚本 | 固件文件 | Qi 芯片版本 |
|------|----------|-------------|
| `zcanpro_qi_iap_log1.py` | `log1.BIN` | V1.1 |
| `zcanpro_qi_iap_log2.py` | `log2.BIN` | V1.2 |

固件文件位于 `python_tools/iap bin/` 目录下。

### 3.2 使用方法

1. 在 ZCANPRO 中打开 CAN 通道（250 kbps, Classical CAN, 扩展帧）
2. 菜单 → 高级功能 → 扩展脚本 → 打开对应脚本
3. 脚本自动执行完整 IAP 流程

### 3.3 IAP 流程

```
1. 读 DID 0x2133（Qi 芯片当前版本）
2. 进入编程会话（0x10 0x02）
3. SecurityAccess 解锁（ECDSA P-256）
4. DID 0x2130 启动 IAP（传固件大小）
5. DID 0x2131 分包发送固件（22 字节/包）
   └ 每包等待 MCU 回 UDS 正响应（含 Qi 芯片 ACK 链路）
   └ NRC 0x72 自动重试 3 次
   └ 包间延时 50ms
6. DID 0x2132 轮询升级状态（120s 超时）
7. 读 DID 0x2133 确认版本更新
```

### 3.4 ACK 链路

MCU 侧实现了非阻塞 ACK 等待机制：

```
CCU (Host)                    MCU                         Qi 芯片
    |                           |                            |
    |-- 2E 2131 addr+data ----->|                            |
    |                           |-- [UART 0xCC 0x02] ------>|
    |                           |    (设 WAIT_ACK 状态)      |
    |                           |<-- [UART ACK status=0x00] -|
    |<-- 6E 2131 (UDS正响应) ---|                            |
    |                           |                            |
    |  (200ms 内无 ACK → NRC 0x72，Host 重试当前包)          |
```

### 3.5 配置常量

```python
QI_IAP_DATA_LEN = 22       # 每包最大数据字节数
QI_IAP_MAX_RETRIES = 3     # NRC 0x72 最大重试次数
QI_IAP_PACKET_DELAY = 0.05 # 包间延时（秒）
STATUS_POLL_INTERVAL = 1.0 # 状态轮询间隔（秒）
STATUS_TIMEOUT = 120       # 升级状态超时（秒）
```

### 3.6 失败对照

| 日志/现象 | 原因 | 处理 |
|-----------|------|------|
| NRC 0x72（重试后仍失败） | Qi 芯片 ACK 超时或 NAK | 检查 UART 接线和 Qi 芯片状态 |
| Qi IAP 升级失败! | Qi 芯片返回 FAILED(0x03) | Qi 芯片固件损坏或不兼容 |
| Qi IAP 超时 (120 秒) | Qi 芯片未返回 COMPLETE | 检查 Qi 芯片是否正常工作 |
| SecurityAccess 解锁失败 | 私钥与 MCU 公钥不匹配 | 换密钥或重编 Bootloader |
| 编程会话进入失败 | CAN 通信问题 | 检查 250kbps 扩展帧通道 |

### 3.7 log1 / log2 同步规则

修改任一脚本的 IAP 逻辑后，必须同步另一个脚本。检查方式：

```bash
diff python_tools/zcanpro_qi_iap_log1.py python_tools/zcanpro_qi_iap_log2.py
```

diff 输出应仅包含以下 4 项差异：
1. 文件头注释中的固件文件名（log1.BIN / log2.BIN）
2. `FIRMWARE_NAME` 常量
3. `EXPECTED_FW_VERSION` 常量
4. 文件头注释中的互斥说明

---

## 4. Qi 调试辅助脚本

### 4.1 zcanpro_qi_read_status.py — 查询 Qi IAP 状态

读取 DID `0x2132`，返回 Qi 芯片 IAP 升级状态（state/progress/版本/已发/总长）。

```bash
# 在 ZCANPRO 中运行
```

### 4.2 zcanpro_qi_read_version.py — 读取 Qi 芯片版本

读取 DID `0x2133`，返回 Qi 芯片固件版本（来自 UART 0x01 上报缓存）。

```bash
# 在 ZCANPRO 中运行
```

### 4.3 zcanpro_qi_set_power.py — 设置 Qi 功率

写入 DID `0x210D`，设置 Qi 芯片功率（500mW=5W / 1000mW=10W / 1500mW=15W）。

```bash
# 在 ZCANPRO 中运行
```

### 4.4 zcanpro_charge_start.py — 启动 Qi 充电

写入 DID `0x2101` 使能充电，读取 DID `0x2102` 确认充电状态。

```bash
# 在 ZCANPRO 中运行
```

---

## 5. MCU 信息读写脚本

### 5.1 zcanpro_read_app_version.py — 读取 APP 版本

读取以下 DID：
- `0xF195` — APP 软件版本（32 字节 ASCII）
- `0xF180` — Bootloader 版本
- `0xF193` — 硬件版本

```bash
# 在 ZCANPRO 中运行
```

### 5.2 zcanpro_sn_write.py — 写入 SN 序列号

写入 DID `0xF18C`，32 字节序列号。需要编程会话 + SecurityAccess 解锁。

```bash
# 在 ZCANPRO 中运行
```

---

## 6. 产线工具

### 6.1 merge_prod_bin.py — 产线镜像合并

将 Bootloader 与 APP 合并为单一产线镜像，用于一次性烧录。

```bash
python python_tools/merge_prod_bin.py

# 显式指定 + 同时生成 HEX
python python_tools/merge_prod_bin.py --hex

# 自定义路径
python python_tools/merge_prod_bin.py \
  --boot qi_wireless_bootloader/mdk_project/Objects/bootloader.bin \
  --app  python_tools/app_bin/app_slot_a.bin \
  --out  python_tools/prod_image.bin
```

#### 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--boot` | `qi_wireless_bootloader/.../bootloader.bin` | Bootloader 二进制 |
| `--app` | `python_tools/app_bin/app_slot_a.bin` | 已签名 APP 镜像 |
| `--out` | `python_tools/prod_image.bin` | 合并输出路径 |
| `--hex` | — | 同时生成 `.hex` 文件 |

#### 输出布局

```
prod_image.bin:
  0x0000  Bootloader  28KB (不足补 0xFF)
  0x7000  Slot A      (256B XATO 头 + 固件)
```

烧录地址：**`0x08000000`**（从 Flash 起始烧）。

#### 约束

| 检查项 | 限制 |
|--------|------|
| Bootloader 大小 | ≤ 28KB (`0x7000`) |
| APP 镜像总长（含头） | ≤ 42KB (`0xA800`) |
| APP 镜像必须含 XATO 头 | 脚本会警告但不阻断 |

### 6.2 verify_image.py — 镜像离线校验

一键验证 XATO 镜像的 6 项完整性检查，与 Bootloader `boot_verify_image()` 逻辑一致。

```bash
python python_tools/verify_image.py python_tools/app_bin/app_slot_a.bin

# 指定公钥
python python_tools/verify_image.py <image_path> --key docs/keys/public.pem
```

#### 检查项

| # | 检查 | 说明 |
|---|------|------|
| 1 | Magic | 首 4 字节必须为 `0x4F544158` ("XATO") |
| 2 | Image Length | 不能为 0，不能超出固件实际大小 |
| 3 | CRC32 | IEEE 802.3 CRC 覆盖裸固件（不含头） |
| 4 | Version | 读取头中版本字符串（信息性） |
| 5 | Build Timestamp | 读取打包时间戳（信息性） |
| 6 | ECDSA P-256 | SHA-256(固件) + OpenSSL 验签 |

#### 依赖

- Python 3.6+
- OpenSSL CLI（签名验证需要，必须在 PATH 中）

### 6.3 sign_seed.py — SecurityAccess 签名生成

手动测试 UDS 0x27 SecurityAccess 时，收到 MCU 返回的 4 字节 seed 后，一键生成 ECDSA P-256 签名及可直接粘贴的 CAN 帧数据。

```bash
python python_tools/sign_seed.py A1B2C3D4

# 指定私钥
python python_tools/sign_seed.py A1B2C3D4 --key docs/keys/private.pem
```

#### 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `seed` | （必填） | MCU 返回的 4 字节 seed，十六进制 |
| `--key` | `docs/keys/private.pem` | ECDSA P-256 私钥 PEM |

#### 输出内容

- ECDSA 签名（64 字节 P1363 格式）
- Method A: `27 02` ISO-TP 多帧（10 帧）
- Method B: `27 03` 分块传输 + `27 02` 触发校验（12 帧）

#### 依赖

- Python 3.6+
- OpenSSL CLI 或 `pip install ecdsa`

---

## 7. 打包脚本依赖链

```
pack_image_slotA.py / pack_image_slotB.py  ──imports──>  zcanpro_ext_ota_slotA_1_1_1.py
     │                                    │
     └─ ECDSA 签名 ──────────────────────┘  （纯 Python，不依赖 ZCANPRO）
     └─ XATO 头构造
     └─ CRC32 计算

verify_image.py    ── 无外部依赖（仅标准库 + OpenSSL CLI）
merge_prod_bin.py  ── 无外部依赖（仅标准库）
sign_seed.py       ── 无外部依赖（仅标准库 + OpenSSL CLI 或 ecdsa）
```

---

## 8. 典型工作流

### 8.1 打包 + 产线烧录

```
Keil Rebuild → fromelf → qi_wireless.bin (裸 bin)
                            │
              python_tools/pack_image_slotA.py (Slot A) / pack_image_slotB.py (Slot B)
                            │
                  app bin/app_slot_a.bin (XATO 头 + 签名)
                            │
                ┌───────────┴───────────┐
                │                       │
    python_tools/verify_image.py   python_tools/merge_prod_bin.py
    (校验 6 项全部 PASS)              │
                                  prod_image.bin
                                        │
                              J-Link / AT32 ISP 烧录 0x08000000
```

### 8.2 开发调试 OTA

```bash
# Slot A → Slot B 升级（最常见）
# 1. Keil 编译 Slot B 固件（IROM1 = 0x08011900）
# 2. 在 ZCANPRO 中运行 zcanpro_ext_ota_slotB_1_1_2.py
# 3. 脚本自动打包 qi_wireless_code_slotB/.../qi_wireless.bin 并升级

# 或者用 auto 脚本
# 1. python pack_image_slotB.py --bin qi_wireless_code_slotB/.../qi_wireless.bin
# 2. 在 ZCANPRO 中运行 zcanpro_ext_ota_auto.py
```

---

## 9. 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| `pack_image_slotA/B` 报找不到固件/私钥 | 默认路径指向 `qi_wireless_code_slotA/...` | 检查 Keil 编译输出路径或用 `--bin`/`--key` 指定 |
| `verify_image.py` 报 Magic 错误 | 输入文件不是 XATO 格式 | 只验证 `app bin/` 下的 bin，不要验证裸 bin |
| `verify_image.py` 报签名全零 | 镜像未签名 | 用 `pack_image` 重新打包 |
| `merge_prod_bin.py` 报 Bootloader 超大 | bootloader.bin > 28KB | 精简 Bootloader 代码 |
| `merge_prod_bin.py` 报 APP 超大 | 打包后 > 42KB | 减小 APP 或调整 IROM1 Size |
| `sign_seed.py` 报签名失败 | openssl 不在 PATH 或私钥不存在 | 安装 OpenSSL / 检查 `docs/keys/private.pem` |
| OTA 脚本报找不到 CAN 通道 | ZCANPRO 未打开通道 | 先打开 250kbps 扩展帧通道 |
| OTA 脚本报 NRC `0x35` | 私钥与 MCU 公钥不匹配 | 换密钥或重编 Bootloader |
| OTA 脚本报 NRC `0x72` | 镜像链接 Slot 与目标槽不符 | 用对应 Slot 的 bin 重新打包 |
| OTA 脚本报 Reset Handler 不在 Slot 内 | Keil IROM1 地址设错 | Slot A = `0x08007100`，Slot B = `0x08011900` |
| Qi IAP NRC 0x72 重试后仍失败 | Qi 芯片 ACK 超时或 NAK | 检查 UART 接线和 Qi 芯片状态 |
| Qi IAP 超时 (120 秒) | Qi 芯片未返回 COMPLETE | 检查 Qi 芯片是否正常工作 |
